# Hyper Regedit Backend

Render Web Service backend API for Hyper Regedit Access.

See `BACKEND_RENDER_GUIDE_BN.md` for Bengali hosting instructions.

Default admin:

```text
admin / ADMIN-2026
```

Required environment variable:

```text
DATABASE_URL=your_neon_postgres_connection_string
```
